import { Component } from "../../core/Component";

export class Navigation extends Component {
  constructor(options) {
    super(options);
    this.tagName = "nav";
    this.className = "nav";
  }
}
